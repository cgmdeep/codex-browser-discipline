# Design Rationale

这份文档说明 `codex-browser-discipline` 为什么这样设计，以及后续怎样继续发展它。它面向维护者和希望理解方法来源的人，不是 Codex 每次执行任务时必须加载的运行说明。

## 起点：问题不只是“会不会点击”

Codex 已经能够识别控件、点击、输入和读取页面，但机械操作正确不等于交互决策合理。最初关注的是三个相互关联的问题：

- 页面布局是否符合人的阅读、判断和操作习惯；
- 点击、跳转、返回和状态变化是否符合直觉；
- 系统是否能在恰当时机解释术语、呈现反馈，并让人知道下一步。

继续分析后发现，悬停解释、仪表盘、表格、表单、画布、AI 工作台等只是表象。若按页面或组件逐个补规则，永远无法覆盖下一种界面。因此，这个 Skill 的核心不是积累 UI 清单，而是建立一套能迁移到陌生界面的决策模型。

## 从界面类型转向人的认知任务

无论页面长什么样，用户通常都在完成若干基本任务：定位、理解、比较、输入、选择、操作、等待、承诺和恢复。Skill 先判断用户正在完成哪类任务，再检查界面和操作是否支持它。

这种抽象能回答一些原本看似零散的问题：

| 表面问题 | 模型中的处理方式 |
| --- | --- |
| 布局不合理 | 检查阅读起点、层级、分组、操作距离和信息密度是否服务当前任务 |
| 点击后的跳转反直觉 | 操作前预测目的地，操作后核验语义目的地和上下文是否保留 |
| 术语需要悬停解释 | 区分补充解释与完成任务所必需的信息，并检查键盘、触控替代 |
| 仪表盘看得到但看不懂 | 分开检查数值显示、指标解释和决策支持 |
| 页面类型从未见过 | 将界面还原为认知任务，而不是等待专用页面规则 |
| AI 长任务显示“完成” | 分离执行状态、结果质量和结果权威性 |
| 删除、发布或授权 | 在承诺边界前核对对象、范围、受众、依赖、后果和恢复路径 |

## 模型如何逐步形成

### 1. 从真实操作开始

先在真实网站上完成任务，记录用户旅程中最早断裂的转换，而不是先写一套理想化规范。早期测试暴露出的关键问题包括上下文丢失、形式成功冒充语义成功、失败后盲目重试，以及高风险操作缺少范围核验。

### 2. 建立语义成功层级

Skill 区分四层结果：

```text
控件被触发
→ 界面发生变化
→ 系统状态正确变化
→ 用户真实目标成立
```

越靠后的证据越接近真正成功。Toast、动画或 URL 变化可以作为证据，但通常不能单独证明任务完成。

### 3. 定义交互契约

每个操作在执行前都尽量明确：

- 目标结果；
- 对象、账户和作用范围；
- 当前假设；
- 成功证据；
- 是否跨越发送、发布、支付、授权、删除等承诺边界；
- 何时应停止并请求用户确认。

这使“点哪个按钮”变成“采取哪个有证据支持、后果可理解的行动”。

### 4. 使用观察—行动—核验循环

```text
观察当前状态
→ 选择最小充分行动
→ 核验语义结果
→ 继续、修正、撤销、补偿或停止
```

这个循环同时适用于网页操作和 UX 评审。前者用它避免游走和误提交，后者用它定位真正破坏用户任务的地方。

### 5. 分开四类状态

复杂任务中容易混淆的状态被拆成四本“账”：

- 情境：当前账户、工作区、对象、筛选和位置；
- 交互：焦点、选择、模式、展开状态和临时输入；
- 执行：任务是否排队、运行、失败或完成；
- 权威：结果是否已验证、批准、发布或可被下游依赖。

特别是 AI 与自动化页面，“执行完成”不自动等于“结果可信”。

### 6. 让验证深度与风险匹配

浏览和筛选允许轻量核验；保存与外发需要更强证据；支付、授权、删除等不可逆操作必须在最终承诺前再次确认对象、范围和后果。这样既不让普通操作过度迟缓，也不把高风险步骤当成普通点击。

## 为什么采用分层文件结构

`SKILL.md` 只保留稳定、通用的决策循环和路由规则；表单、数据界面、异步 AI、正式评审等细则放在 `references/` 中按需读取。

这种结构有两个目的：

1. 日常网页任务不必加载整套方法，减少上下文和注意力成本；
2. 某一领域的经验可以独立演进，而不把偶发问题膨胀成全局规则。

本文件与 `README.md`、`VALIDATION.md` 属于说明和维护材料，不应被默认加入 Skill 的运行上下文。

## 证据驱动的迭代方式

新增规则时遵循以下路径：

```text
真实交互
→ 记录最早断裂的任务转换
→ 区分直接观察、合理推断和未验证假设
→ 判断是否可跨界面复现
→ 用最窄的通用规则修正
→ 做迁移测试和回归测试
→ 达到阈值后冻结版本
```

一次偶发失败不应直接变成普遍规则。只有问题在不同页面、不同任务或不同模型上重复出现，并且现有原则无法清楚处理时，才值得提升为核心规则。具体测试结果和升级阈值见 [VALIDATION.md](VALIDATION.md)。

## 取舍与边界

这个 Skill 有意不做以下事情：

- 不替代真实用户研究、可用性测试和无障碍专项测试；
- 不以个人审美代替证据，也不尝试建立穷尽式组件规范；
- 不把领域事实、法律要求或业务权限交给通用交互规则判断；
- 不因自动化能力存在，就默认有权跨越外发或不可逆承诺边界。

它真正要改善的，是 Codex 在网页上做决定的质量：知道该看什么、为什么点击、什么才算成功、失败后怎样恢复，以及什么时候必须停下来。
