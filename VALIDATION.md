# Validation Report — v0.2

## 结论

`codex-browser-discipline` 已获得足够证据进入长期真实使用阶段。验证重点不是确认 Codex 能否点击按钮，而是检查这套方法能否跨不同界面稳定改变以下决策：

- 先定义语义目标和停止条件；
- 核对账户、对象、范围和关键假设；
- 区分界面变化、系统状态和任务真正完成；
- 在错误、返回、等待和中断后保留上下文；
- 在高后果操作前识别授权与承诺边界；
- 用直接观察、来源、推理和未测边界约束评审结论。

现有结果表明它已经具有迁移能力和实际诊断能力。继续增加相似页面的边际价值较低，因此 v0.2 不再为穷举组件或产品类型主动扩张规则。

## 覆盖矩阵

| 场景 | 代表界面或实验 | 主要验证内容 | 结果 |
| --- | --- | --- | --- |
| 搜索与知识页 | Wikipedia | 定向、输入校正、直接命中、结果列表、下钻和返回 | 发现不同搜索路径的上下文保留差异 |
| 本地任务管理 | TodoMVC | 新增、完成、筛选、刷新和恢复 | 核心路径可被通用模型解释 |
| 商业流程 | SauceDemo | 表单名称、错误恢复、购物车、复核和完成凭证 | 发现语义名称、逐项错误与复核信息问题 |
| 仪表盘 | 运营仪表盘示范与盲测 | 指标解释、时间范围、比较、下钻、筛选和复位 | 验证“显示—解释—决定”分层 |
| 数据表格 | Handsontable | 表头语义、选择、排序、筛选与可见/全部范围 | 证明对象选择不等于数据转换 |
| 画布编辑器 | Excalidraw | 模式、选择、焦点、视点、撤销历史和持久化 | 发现内容保留与历史保留不能互相替代 |
| AI 与异步任务 | AI 监督台、流式聊天实验 | 排队、运行、停止、失败、重试、部分结果和可信度 | 验证执行状态与结果权威性双轴模型 |
| 高风险删除 | GitHub 仓库删除流程 | 对象、后果、渐进确认、最终门槛、Esc 与焦点返回 | 正确停在最终副作用之前 |
| 权限和可见性 | GitHub public → private 流程 | 影响清单、关联 Pages、最终按钮、取消和焦点恢复 | 发现依赖对象遗漏与取消后焦点丢失 |

## 代表性真实发现

测试不是为了证明页面“都没问题”。它实际识别出了下面这些不同层级的问题：

- Wikipedia 直接命中和结果列表路径在返回时保留的查询上下文不同。
- 表格中选中表头、排序和筛选是三个不同操作，视觉高亮不能证明数据已变换。
- 画布刷新后可以保留内容，同时清空撤销历史；持久化成功不等于恢复能力保留。
- AI 请求可能已经执行完成，但结果仍是暂定或待核验状态。
- 删除流程的渐进确认可以工作，但完整后果出现得较晚。
- GitHub 可见性对话框遗漏当前 Pages 站点这一关联对象；取消后焦点也没有返回触发控件。

这些发现覆盖语义、连续性、恢复、辅助操作和高风险后果，说明模型不仅能描述原则，也能在真实页面中定位具体断点。

## 独立视角验证

- 使用 GPT-5.5 对 AI 任务监督界面进行独立评审，并根据发现修正了单选语义、焦点和完成权威性问题。
- 使用陌生公共网站进行迁移测试，验证模型没有依赖演示页面的既有结构。
- 对早期前向测试进行一致性校准，将“证据依据、判断类别、优先级、未测边界”拆成独立维度，减少事实和推断混写。

这些结果提供了有价值的外部视角，但不构成统计意义上的评审者间信度研究。

## 风险分层

| 层级 | 任务 | 最小验证范围 |
| --- | --- | --- |
| A | 阅读、导航、查找和比较 | 定向、主路径、语义目的地、必要的返回 |
| B | 搜索、筛选、选择和未保存编辑 | A + 错选或空状态、复位、状态可见性、键盘路径 |
| C | 保存、上传、画布、AI 和队列任务 | B + 中断、失败、撤销或停止、刷新/重开和恢复范围 |
| D | 发送、发布、支付、授权、删除和权限变化 | C + 对象/范围/账户、依赖对象、授权边界、目的地和凭证或补偿 |

风险层级只决定验证深度，不授予执行权限。

## 保留边界

- 未系统覆盖真实付款、医疗、法律和金融领域语义。
- 未使用多种真实屏幕阅读器、触控设备和长期现场数据验证。
- 没有为了评审执行仓库删除、权限变化等不可逆副作用，因此提交后凭证和补偿路径仍是边界。
- 单次演示和独立模型评审不能替代真实用户研究、可用性实验或生产分析数据。

这些边界不阻止日常使用；它们限制的是可以对普适性、无障碍覆盖和高风险专业领域作出的声明。

## 后续升级门槛

只有以下证据之一出现时，才修改核心规则：

1. 同一决策错误在不同产品或任务中重复出现；
2. 新证据改变授权、承诺、停止或语义成功的判断；
3. 独立评审者持续在同一事实或最早断点上产生不可解释的分歧；
4. 标准、用户研究或真实事故证明现有规则不足。

一次性视觉偏好、单个网站的产品限制或偶发异常保留在具体评估报告中，不自动升级为通用 Skill 规则。
